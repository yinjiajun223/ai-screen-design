# AI Screen Design

基于 Vue 3 的可视化大屏设计编辑器原型。项目采用物料、页面 DSL 和编辑器状态分离的方式，支持将物料拖入画布后进行选择、移动、缩放和属性编辑。

## 当前功能

- 编辑器基础布局：物料面板、图层面板、画布和属性面板
- 面板显隐切换，支持物料、图层和属性面板独立开关
- 物料拖拽：从物料面板拖入画布创建节点
- 当前物料分类：信息、图表
- 当前已注册物料：文本、柱状图物料定义
- 节点选择：单选、框选、多选和点击画布取消选择
- 节点操作：拖动、调整尺寸、批量移动和批量缩放
- 画布辅助：可缩放标尺、画布尺寸设置、背景色设置和窗口尺寸自适应
- 图层管理：选择节点、拖动调整图层顺序
- 节点右键菜单：复制、移除、置顶、置底、锁定和解锁
- 节点属性编辑：文本内容、颜色、位置和尺寸
- 撤销与重做：支持属性表单修改的撤销和重做，按钮会根据历史记录自动启用或禁用
- 深色编辑器主题和 Element Plus 控件样式适配

## 技术栈

- Vue 3 + TypeScript + `<script setup>`
- Vite
- Pinia
- Element Plus
- Tailwind CSS
- Iconify
- vue3-moveable：节点移动和缩放
- vue3-selecto：框选和多选
- vue3-sketch-ruler：画布标尺和缩放
- vue-draggable-plus：图层排序

## 快速开始

项目使用 pnpm 管理依赖，建议使用 Node.js 22.18+ 或 Node.js 24.12+。

```bash
pnpm install
pnpm dev
```

启动后打开终端输出的本地地址，通常为 `http://localhost:5173`。

### 常用命令

```bash
# 类型检查和生产构建
pnpm build

# 仅执行类型检查
pnpm type-check

# 预览生产构建结果
pnpm preview

# 运行代码检查
pnpm lint
```

## 使用流程

1. 从左侧物料面板选择物料，将物料拖入中间画布。
2. 点击节点进行选择，拖动节点调整位置，使用控制点调整尺寸。
3. 使用 Shift 框选或多选节点，可批量移动和缩放。
4. 在右侧属性面板修改节点内容、颜色、位置和尺寸；未选中节点时可修改画布尺寸和背景色。
5. 在图层面板中选择节点或拖动节点调整层级。
6. 右键节点执行复制、删除、置顶、置底和锁定操作。
7. 使用顶部撤销、重做按钮回退或恢复属性修改。

## 项目结构

```text
src/
├─ editor/                  编辑器布局、工具栏、画布和面板
│  ├─ canvas/               画布、标尺、选择和移动逻辑
│  └─ panels/               物料、图层、属性面板及表单组件
├─ materials/               物料注册和物料组件
├─ schema/                  页面、画布和节点 DSL 类型
├─ stores/                  Pinia 编辑器状态
├─ composables/             可复用逻辑，例如撤销重做
├─ styles/                  全局样式和主题变量
└─ utils/                   DSL 属性读写等工具函数
```

物料通过 `src/materials/*/index.ts` 注册。每个物料可以提供节点 schema、渲染组件和属性 setter，编辑器会通过注册表动态获取物料和对应组件。

## 开发进展

项目提交记录中的主要阶段如下：

- 2026-07：完成 Vue 3 项目初始化、编辑器布局、工具栏、物料面板和图层面板。
- 2026-07-30：完成物料 DSL、节点拖放渲染、节点移动和缩放。
- 2026-08-04：加入多节点选择、画布标尺、图层拖拽排序和 ResizeObserver 自适应。
- 2026-08-05：完成页面/画布 DSL、画布属性面板和节点属性面板，并统一编辑器主题样式。
- 2026-08-06：加入节点右键菜单及复制、删除、层级、锁定操作。
- 2026-08-07：实现基于命令记录的撤销和重做状态管理。

## 当前限制

顶部右侧的预览、查看 JSON、发布、导入和导出按钮目前仍为占位操作；数据暂未接入持久化存储，刷新页面后编辑内容会丢失。图表物料已完成元数据注册，具体图表渲染组件仍需继续接入。
